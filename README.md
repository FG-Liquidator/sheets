# sheets

Rendered bid sheets from [lot-eval](https://github.com/FG-Liquidator/lot-eval), served by GitHub Pages.
HTML only — no code, no raw lot data, no credentials. `noindex` + `robots.txt` keep it out of search.
Regenerate with `python3 -m lot_eval publish <auction_id>` from the lot-eval checkout.
